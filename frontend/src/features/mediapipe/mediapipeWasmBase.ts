/** Must match the installed @mediapipe/tasks-vision version (see package-lock.json). */
export const MEDIAPIPE_TASKS_VISION_VERSION = "0.10.35";

export const MEDIAPIPE_WASM_BASE = `https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@${MEDIAPIPE_TASKS_VISION_VERSION}/wasm`;
